---
import { getCollection } from 'astro:content';
import { Header } from '@components/astro';
import ProjectsList from '@components/astro/pages/projects/ProjectsList.astro';
import Layout from '@layouts/Base.astro';

const collection = await getCollection('projects', ({ data }) => !data.hidden);

const title = 'Реализованные проекты';
const description = 'Реализованные проекты';
---

<Layout
  title={`${title} • НИЦ «Строительная экспертиза»`}
  description={`${description} • АО НИЦ «Строительная Экспертиза»`}>
  <Header {title} />

  <main
    class='frame'
    itemprop='mainContentOfPage'>
    <ProjectsList {collection} />
  </main>
</Layout>
